Спотовый межбиржевой арбитражный бот
Цель проекта

Проект решает задачу спотового межбиржевого арбитража без HFT:
система обнаруживает ценовые расхождения по одинаковым торговым парам на разных биржах, отбирает только ликвидные и технически исполнимые ситуации и готовит данные для исполнения сделок.

Главные приоритеты:

Корректность исполнения (арбитраж возможен технически и экономически).

Прозрачная архитектура — разделение транспорта, нормализации, проверки цен и логики принятия решения.

Только затем — скорость и оптимизация, включая возможный перенос критических участков на WebSocket / Go.

Архитектура: уровни
1. Источники данных (REST, Stage-1)

На первом этапе система работает через REST-эндпоинты бирж:

каждая биржа имеет собственный модуль в src/exchanges/...;

запросы выполняются асинхронно через httpx.AsyncClient;

транспортный слой не содержит бизнес-логики.

Реализовано:

Binance

/api/v3/ticker/bookTicker — best bid/ask

/api/v3/ticker/24hr — 24-часовая статистика и объёмы

Bybit

/v5/market/tickers?category=spot

Задача слоя — получить сырые данные без интерпретации.

2. Нормализация торговых пар

Используется единый внутренний формат имён:

BASE_USDT
например: BTC_USDT, ETH_USDT


Нормализация выполняет:

унификацию названий между биржами;

построение таблицы соответствия;

фильтрацию по ликвидности.

Применяется порог:

MIN_24H_VOLUME_USDT


Если суточный оборот пары на бирже ниже порога —
пара на этой бирже считается неактивной и не используется в расчётах.

3. Матрица цен

Рабочая структура для одного цикла сканирования:

{
  "BTC_USDT": {
    "binance": {"bid": ..., "ask": ...},
    "bybit":   {"bid": ..., "ask": ...}
  },
  ...
}


Матрица формируется батч-срезом рынка, а не по одной паре.

4. Исполняемая инфраструктура (main.py)

Запускаются два независимых процесса:

🟦 PairsNormalizer

собирает пары с бирж;

фильтрует по обороту и доступности цены;

формирует нормализованный список;

результат записывается в shared["pairs"];

выполняется раз в 60 секунд.

🟨 PriceSpreadWatcher (Stage-1 Price Snapshot)

читает пары из shared["pairs"];

асинхронно получает тикеры с обеих бирж;

строит snapshot цен;

считает процентный спред между биржами;

кандидаты по спреду записываются в лог-файл:

logs/signals_YYYY-MM-DD.log


В stdout выводится только:

дата и время события,

направление сделки,

bid / ask,

спред в процентах.

5. Stage-1 — проверка спреда (price-only)

Алгоритм:

берём нормализованные пары;

загружаем цены обеих бирж;

считаем:

spread = sell_price – buy_price
spread_pct = spread / buy_price * 100


если спред превышает порог — фиксируем сигнал и логируем;

на этом этапе ликвидность в стакане ещё не проверяется.

⚠️ КРИТИЧЕСКИЙ ЭТАП НОРМАЛИЗАЦИИ:
🔴 ПРОВЕРКА ПЕРЕВОДИМОСТИ АКТИВА МЕЖДУ БИРЖАМИ

Большая часть “аномальных спредов” возникает у монет, которые нельзя физически переместить между биржами.

Примеры причин:

депозиты или выводы отключены на одной из бирж;

актив торгуется в разных сетях блокчейна;

токен на одной бирже — IOU / wrapped / bridged-версия;

монета представлена как производный токен (staked / synthetic).

В таких случаях:

купить на бирже A → перевести → продать на бирже B
НЕВОЗМОЖНО ТЕХНИЧЕСКИ


поэтому спред не выравнивается рынком.

✔️ Что именно проверяем (строго по пунктам)

Для каждого актива на этапе нормализации необходимо убедиться:

депозит разрешён на обеих биржах

вывод разрешён на обеих биржах

список доступных сетей пересекается
(существует хотя бы одна одинаковая сеть перевода)

актив одинакового типа на обеих биржах
— не wrapped, не IOU, не derivative-token

Только в этом случае пара допускается в арбитражную матрицу.

API-источники (этап планируется)

Binance — /sapi/v1/capital/config/getall

Bybit — /v5/asset/coin/query-info

Требуются API-ключи, планируется кэширование результатов проверки.

🟡 Stage-2 — Depth-Check (планируемый обязательный этап)

Stage-2 выполняется только после Stage-1 сигнала.

Задача — подтвердить, что спред можно реально исполнить с учётом ликвидности.

Проверяем:

загружаем стакан (order-book) на обеих биржах;

оцениваем объём доступный в верхних уровнях;

считаем реальный исполняемый спред
с учётом глубины и возможного проскальзывания;

сравниваем результат с порогом стратегии;

сигнал подтверждается только при достаточном объёме.

Таким образом:

Stage-1 — быстрая ценовая проверка;

Stage-2 — проверка реальной исполнимости.

Текущий статус проекта

✔ транспортные клиенты Binance / Bybit
✔ нормализация пар и фильтр по обороту
✔ multiprocessing-оркестратор
✔ Stage-1 ценовой анализ
✔ логирование сигналов в файлы

🚧 в процессе и запланировано:

добавление проверки переводимости активов (нормализация);

реализация Stage-2 Depth-Check;

кэш сетей и статусов депозита/вывода;

возможный переход на WebSocket-стримы.

Технологический стек

Python 3.12

uv

asyncio

httpx

multiprocessing

logging (сигналы → файлы)

pandas / numpy — по мере необходимости

Структура проекта
crypto_arbitrage/
│ README.md
│ main.py
│ pyproject.toml
│ uv.lock
│
├─ logs/                 # сигналы Stage-1 (игнорируются в Git)
│
└─ src/
   ├─ config.py
   ├─ exchanges/
   │   ├─ binance/binance_market.py
   │   └─ bybit/bybit_market.py
   └─ utils/
       ├─ pairs_normalize.py
       └─ stage1_price_snapshot.py

Резюме

Система уже выполняет:

нормализацию пар,

фильтрацию по обороту,

построение ценовой матрицы,

детекцию первичных арбитражных сигналов,

логирование для аналитики.

Следующие два ключевых шага:

Проверка переводимости активов на этапе нормализации

Stage-2 Depth-Check для подтверждения исполнимости

Эти этапы критически важны для перехода от прототипа к реальной стратегии.