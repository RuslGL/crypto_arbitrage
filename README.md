Спотовый межбиржевой арбитражный бот
Цель проекта

Система выполняет спотовый межбиржевой арбитраж без HFT и без попытки торговать в “микросекундных” окнах.
Фокус — на ситуациях, которые:

технически исполнимы,

экономически оправданы,

проходят проверку ликвидности и инфраструктурных ограничений.

Проект постепенно развивается от аналитического прототипа к торговому движку.

Главные принципы:

корректность расчётов важнее скорости;

архитектура разделена на независимые слои;

транспорт, нормализация, проверка цен и принятие решений — изолированы;

оптимизация и WebSocket-стримы рассматриваются только после стабилизации логики.

Архитектура — уровни
1) Источники данных (REST, Stage-1)

На текущем этапе система использует REST-эндпоинты спотовых рынков и асинхронные запросы через httpx.AsyncClient.

Для каждой биржи существует отдельный модуль:

src/exchanges/<exchange>/


В транспортном слое отсутствует бизнес-логика — он только получает сырые данные.

Реализовано:

Binance

/api/v3/ticker/bookTicker — best bid / ask

/api/v3/ticker/24hr — объёмы и ценовая статистика

Bybit

/v5/market/tickers?category=spot

Задача слоя — получить данные в неизменном виде.

2) Нормализация торговых пар

Единый внутренний формат имён пар:

BASE_USDT
например: BTC_USDT, ETH_USDT


Нормализация выполняет:

унификацию названий между биржами,

формирование таблицы соответствий,

фильтр по ликвидности.

Применяется порог:

MIN_24H_VOLUME_USDT


Если суточный оборот меньше порога —
пара на данной бирже считается неактивной и исключается из расчётов.

3) Матрица цен рынка

В ходе одного цикла сканирования формируется структура:

{
  "BTC_USDT": {
    "binance": {"bid": ..., "ask": ...},
    "bybit":   {"bid": ..., "ask": ...}
  }
}


Матрица строится батч-срезом рынка, а не покомпонентно по парам —
это уменьшает сетевые накладные расходы и снижает расхождения по времени получения данных.

4) Исполняемая инфраструктура (main.py)

Используется multiprocessing-оркестратор и независимые процессы.

🟦 PairsNormalizer

собирает пары с бирж,

накладывает фильтр по обороту,

формирует нормализованный список,

результат сохраняется в shared["pairs"],

обновление выполняется раз в 60 секунд.

🟨 PriceSpreadWatcher (Stage-1 snapshot)

читает shared["pairs"],

получает bid/ask с обеих бирж,

строит ценовой snapshot,

считает спред между биржами,

отбирает пары выше порога доходности,

записывает сигналы в CSV-лог.

Stage-1 — ценовая проверка (price-only)

Алгоритм:

берём нормализованные пары,

получаем bid/ask обеих бирж,

считаем:

spread     = sell_price – buy_price
spread_pct = spread / buy_price * 100


Если spread_pct >= MIN_PROFIT_PCT
→ пара становится кандидатом на Stage-2
→ сигнал записывается в лог.

На этом этапе стакан ещё не проверяется.

🚧 Этап в процессе: проверка переводимости актива

Большая доля “выгодных спредов” связана с монетами,
которые физически нельзя перевести между биржами.

Причины:

отключены депозиты / выводы;

разные сети токена и отсутствует пересечение;

wrapped / synthetic / staked токены;

IOU-версии актива.

В таких случаях арбитраж реально невыполним,
поэтому такие пары должны отсеиваться на этапе нормализации.

Будем проверять:

депозиты включены на обеих биржах;

вывод включён на обеих биржах;

сети перевода имеют пересечение;

актив идентичен по типу (не wrapped / не synthetic).

Источники API (планируется):

Binance — /sapi/v1/capital/config/getall

Bybit — /v5/asset/coin/query-info

Будет добавлено кэширование и периодическая синхронизация статусов.

🟡 Stage-2 — Depth-Check (запланирован)

Выполняется только для сигналов Stage-1.

Задача:

загрузить стаканы обеих бирж,

оценить доступный объём на верхних уровнях,

рассчитать исполнимую среднюю цену,

учесть проскальзывание и ограничение по глубине,

подтвердить реальность выполнения сделки.

Тем самым:

Stage-1 — ценовая аномалия
Stage-2 — фактическая исполнимость

Текущий статус

✔ транспортные клиенты Binance / Bybit
✔ нормализация пар и фильтр по обороту
✔ multiprocessing-оркестратор
✔ Stage-1 ценовой анализ
✔ логирование сигналов в файлы

🚧 в работе:

проверка переводимости активов,

Stage-2 depth-check,

кэш статусов депозитов/сетей,

возможный переход на WebSocket-стримы.

Технологический стек

Python 3.12

uv

asyncio / httpx

multiprocessing

CSV-логирование

(pandas / numpy — при необходимости)

Структура проекта
crypto_arbitrage/
│ README.md
│ main.py
│ pyproject.toml
│ uv.lock
│
├─ logs/
│
└─ src/
   ├─ config.py
   ├─ exchanges/
   │   ├─ binance/binance_market.py
   │   └─ bybit/bybit_market.py
   └─ utils/
       ├─ pairs_normalize.py
       └─ stage1_price_snapshot.py

Резюме

Текущая версия системы уже выполняет:

нормализацию и отбор активных торговых пар,

фильтрацию по ликвидности,

построение рыночного ценового среза,

детекцию первичных арбитражных сигналов,

запись сигналов для аналитики и тестирования стратегии.

Ближайшие ключевые шаги:

проверка переводимости активов на этапе нормализации;

внедрение Stage-2 Depth-Check и подтверждение исполнимости сделок.

Эти этапы критичны для перехода от аналитического прототипа
к практической торговой системе.



#################################
#########################
###############
###########
######
####
##
#

✅ ЭТАП 1 — Добавляем модуль биржи (market-collector)

📂 Создаём папку:

src/exchanges/<exchange_name>/


Минимальный набор файлов:

__init__.py
<exchange_name>_market.py


В файле *_market.py обязательно реализуем три функции:

fetch_tickers_24h_raw()      — для нормализации пар
fetch_book_tickers_raw()     — для Stage-1 (bid/ask)
fetch_orderbook_raw(symbol)  — для Stage-2 (depth-check)


Функции должны возвращать сырые данные без обработки.

✅ ЭТАП 2 — Добавляем нормализацию символов

📂 Новый файл:

src/utils/<exchange_name>_pairs_normalize.py


Там реализуем:

async def build_pairs_<exchange_name>():
    # получаем tickers
    # фильтруем USDT-пары
    # проверяем объем
    # приводим к "BASE_USDT"
    # возвращаем словарь:
    # { "BTC_USDT": "<NATIVE_SYMBOL>" }


Только нормализация — без матчинга с другими биржами.

✅ ЭТАП 3 — Включаем биржу в общий нормализатор

Файл:

src/utils/pairs_normalize.py


Добавляем:

импорт функции новой биржи

вызов её нормализации

объединение в общий словарь

Результат должен иметь формат:

{
  "BTC_USDT": {
    "binance": "BTCUSDT",
    "bybit":   "BTCUSDT",
    "<exchange>": "<NATIVE_SYMBOL>" or None
  }
}


⚠️ Если биржи нет → None, не удаляем пару.

✅ ЭТАП 4 — Подключаем биржу в Stage-1 (ценовой срез)

Файл:

src/utils/stage1_price_snapshot.py


Добавляем:

импорт fetch_book_tickers_raw

преобразование данных в общий формат

участие в расчёте спреда

Объект пары должен хранить:

{
  "<exchange>": { bid, ask }
}


И логика выбора лучшего направления должна учитывать ещё одну сторону рынка.

✅ ЭТАП 5 — Подключаем биржу в Stage-2 (depth-check)

Файл:

main.py   (TradeWorker)


В _handle_signal():

добавляем импорт fetch_orderbook_raw

добавляем вызов стакана новой биржи

учитываем новый вариант маршрута:

A → B
A → C
B → C
C → A
...


Выбор BUY / SELL = по направлению сигнала.

✅ ЭТАП 6 — Логи и метрики (без изменений кода логики)

Только расширяем поля:

buy_exchange
sell_exchange


Формат логов остаётся прежним.

✅ ЭТАП 7 — Конфиг

В src/config.py добавляем только константы эндпоинтов и базовый URL:

<EXCHANGE>_BASE_REST_URL
<EXCHANGE>_TICKERS_ENDPOINT
<EXCHANGE>_ORDERBOOK_ENDPOINT
...